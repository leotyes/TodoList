package com.example.todolist

import androidx.room.Entity
import java.sql.Time
import java.util.Date

//data class GroupInfo(val title: String, val items: Array<TodoItemInfo>)
//data class TodoItemInfo(val title: String, val description: String, val due: Date, val timeStart: Time, val timeEnd: Time)